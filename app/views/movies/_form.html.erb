<%= form_with(model: movie) do |form| %>
  <% if movie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
        <% movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, "Título:", style: "display: block" %>
    <%= form.text_field :title %>
  </div>
  <br>
  <div>
    <%= form.label :release_year, "Ano de Lançamento:", style: "display: block" %>
    <%= form.number_field :release_year, min: 1 %>
  </div>
  <br>
  <div>
    <%= form.label :actor_ids, "Elenco de Atores:", style: "display: block" %>
    <%= form.collection_check_boxes :actor_ids, @actors, :id, :name %>
  </div>
  <br>
  <div>
    <%= form.submit %>
  </div>
<% end %>
